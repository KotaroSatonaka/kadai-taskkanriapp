<h1>id = <%= @task_list.id %> のタスク詳細ページ</h1>

<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td><%= @task_list.id %></td>
  </tr>
  <tr>
    <th>タイトル</th>
    <td><%= @task_list.title %></td>
  </tr>
  <tr>
    <th>タスク</th>
    <td><%= @task_list.content %></td>
  </tr>
</table>

<%= link_to 'このタスクを編集する', edit_task_list_path(@task_list), class: 'btn btn-default' %>
<%= link_to 'このメッセージを削除する', @task_list, method: :delete, data: { confirm: '本当に削除してよろしいですか？' }, class: 'btn btn-danger' %>